pluginGroup = dev.provenance.recorder
pluginName = provenance-recorder
pluginVersion = 0.1.0
# Since 2025.3 IntelliJ IDEA is a single unified distribution, resolved via the
# intellijIdea(...) dependency helper (the legacy intellijIdeaCommunity/IC helper
# only covers pre-2025.3). We target the plugin at com.intellij.modules.platform
# so it still loads across JetBrains IDEs. since-build matches the platform (261).
pluginSinceBuild = 261
platformVersion = 2026.1.4
