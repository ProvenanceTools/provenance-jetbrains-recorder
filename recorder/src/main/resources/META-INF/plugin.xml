<idea-plugin>
    <id>com.aaryanmehta.provenance.recorder</id>
    <name>Provenance Recorder</name>
    <vendor email="aaryanm@berkeley.edu" url="https://aaryanmehta.com">Aaryan Mehta</vendor>

    <description><![CDATA[
    <p>
      Provenance Recorder keeps an honesty log of how your code came together
      while you work on a course assignment. You submit it alongside your code
      so your work can be reviewed as a <em>process</em>, not just a final file.
    </p>
    <p>
      It only runs in assignment folders that have been authorized for
      recording &mdash; a project containing a valid, course-signed
      <code>.provenance-manifest</code>. In every other project the plugin does
      nothing: no recording, no indicator, no UI.
    </p>
    <p><b>What you'll see</b></p>
    <ul>
      <li>A <b>&ldquo;Provenance: recording&rdquo;</b> indicator in the status
          bar whenever the plugin is active &mdash; the only visible change. No
          popups, no toolbars, no slowdowns.</li>
      <li>A hidden <code>.provenance/</code> folder in the assignment where the
          log lives. Don't delete, commit, or edit it &mdash; the seal command
          bundles it for you.</li>
    </ul>
    <p>
      Recording is meant to be fully transparent &mdash; there are no hidden
      signals. While you work in an authorized assignment, the plugin writes a
      timestamped, tamper-evident log of the editing process. Here is
      <b>everything</b> it captures:
    </p>
    <p><b>Your files and edits</b></p>
    <ul>
      <li>The full text of every edit you make in the assignment files &mdash;
          the exact characters inserted or deleted, and where.</li>
      <li>The contents of an assignment file when it is opened (for files up to
          64&nbsp;KB; larger files are recorded as a cryptographic hash plus a
          line count).</li>
      <li>A cryptographic hash of each file every time you save.</li>
    </ul>
    <p><b>Pastes</b></p>
    <ul>
      <li>Every paste into an assignment file: its location, length, a hash, and
          the pasted text itself (its beginning and end for very large pastes)
          &mdash; what makes it possible to tell which code came from outside
          your own typing.</li>
    </ul>
    <p><b>Where your attention is</b></p>
    <ul>
      <li>Which file is focused and when you switch between files; your cursor
          position and any text you select.</li>
      <li>When the IDE window gains or loses focus, and how long you've been idle
          (sampled on a periodic heartbeat).</li>
    </ul>
    <p><b>Terminal</b></p>
    <ul>
      <li>Commands you run in the IDE's integrated terminal, including the
          command text and its exit code, plus which shell you're using.</li>
    </ul>
    <p><b>Your environment</b></p>
    <ul>
      <li>A note whenever a plugin activates while you're working.</li>
      <li>Your IDE version and operating-system platform, and the recorder's own
          version.</li>
      <li>A one-way fingerprint of your machine &mdash; not your name, email, or
          IP address, just a value used to notice if one submission was recorded
          across different computers.</li>
    </ul>
    <p><b>Changes made outside your typing</b></p>
    <ul>
      <li>When an assignment file changes by something other than your
          keystrokes &mdash; a formatter, a script, git, or an external editor
          &mdash; the before/after hashes, the size of the change, and (for
          small files) the new contents.</li>
      <li>Git operations such as commits and checkouts.</li>
    </ul>
    <p><b>Session and integrity bookkeeping</b></p>
    <ul>
      <li>Session boundaries and the links between sessions when you reopen the
          project, the assignment manifest's signature, and a per-session
          signing key that makes the log tamper-evident.</li>
      <li>Internal health signals: clock changes, paste anomalies, breaks in the
          log's hash chain, and any time the recorder enters a degraded mode or
          recovers a corrupted log.</li>
    </ul>
    <p>
      The log is stored <b>only on your computer</b> until you upload the sealed
      <code>.zip</code> yourself; the plugin makes <b>no network requests</b>.
    </p>
    <p><b>Submitting your work</b></p>
    <p>
      When you're done, run <b>Tools &rarr; Provenance: Prepare Submission
      Bundle</b>. A sealed <code>.zip</code> is produced next to your
      assignment &mdash; upload it with your code per your assignment's
      instructions.
    </p>
    ]]></description>

    <change-notes><![CDATA[
    <p><b>0.2.1</b> &mdash; reliability fixes. No change to what is recorded.</p>
    <ul>
      <li>Fixed: on Windows, the recorder did not start at all for a project stored on the
          <b>WSL filesystem</b> &mdash; no recording, no indicator, and no
          &ldquo;Prepare Submission Bundle&rdquo; command. It now records there normally.</li>
      <li>Fixed: an <b>&ldquo;IDE error occurred&rdquo;</b> notification could appear when
          opening a project.</li>
      <li>Fixed: a file that was <b>already open</b> when recording began could be recorded
          without its starting contents, so a reviewer's replay of that file started from an
          empty page instead of the code you began with.</li>
      <li>The status bar now tells you when recording has <b>stopped working</b>, instead of
          continuing to show the normal &ldquo;recording&rdquo; indicator. If you ever see it,
          tell your course staff &mdash; it means the log is incomplete.</li>
      <li>Preparing a submission bundle now reports a clear error instead of failing
          quietly, and no longer leaves a stray temporary file behind.</li>
      <li>Fixed: sealed bundles reported a recorder fingerprint that course staff's checker
          did not recognize, which flagged every submission for manual review.</li>
    </ul>
    <p><b>0.2.0</b></p>
    <ul>
      <li>Records <b>every authorized assignment open in the project at once</b>, including
          assignment folders nested inside a parent project, instead of only the project root.
          Each assignment gets its own independent log, and the status-bar indicator shows how
          many are recording.</li>
      <li><b>Tools &rarr; Provenance: Prepare Submission Bundle</b> now asks which assignment to
          seal when more than one is recording.</li>
      <li>Opening a file records up to <b>64&nbsp;KB</b> of its contents inline (was 4&nbsp;KB),
          so more of your starting point is captured directly rather than as a hash.</li>
      <li>Suspending your machine no longer looks like unexplained missing time: the recorder
          marks the gap when the session resumes.</li>
      <li>Fixed: saving a file could be misreported as a change made outside your editor.</li>
      <li>Fixed: a paste is never recorded in a form the reviewer's replay cannot reproduce.</li>
    </ul>
    <p><b>0.1.0</b> &mdash; first release.</p>
    <ul>
      <li>Records the editing process in course-authorized assignment folders and seals it
          into a tamper-evident submission bundle via
          <b>Tools &rarr; Provenance: Prepare Submission Bundle</b>.</li>
      <li>Bundles are format-compatible with the Provenance VS Code recorder.</li>
    </ul>
    ]]></change-notes>

    <depends>com.intellij.modules.platform</depends>

    <!--
      Optional, structurally-gated wiring (Plan 7). Each config-file is loaded ONLY when
      the named plugin is present; the classes it registers reference that plugin's types
      directly, so they are never classloaded on an IDE lacking the plugin (avoiding
      NoClassDefFoundError — the documented pattern, stronger than a runtime guard).
      Terminal/Git absence therefore degrades gracefully: the signal is simply not emitted.
    -->
    <depends optional="true" config-file="provjet-terminal.xml">org.jetbrains.plugins.terminal</depends>
    <depends optional="true" config-file="provjet-git.xml">Git4Idea</depends>

    <extensions defaultExtensionNs="com.intellij">
        <!-- Balloon group for disk-full degraded-mode disclosure (PRD §4.8). Resolved by id
             string directly (DegradedModeNotifier.GROUP_ID); no NotificationGroupManager lookup. -->
        <notificationGroup id="Provenance Recorder" displayType="BALLOON"/>
        <postStartupActivity implementation="dev.provenance.recorder.activation.RecorderActivationActivity"/>
        <statusBarWidgetFactory
            id="ProvenanceRecordingWidget"
            implementation="dev.provenance.recorder.statusbar.RecordingStatusBarWidgetFactory"
            order="first"/>
        <editorActionHandler
            action="EditorPaste"
            implementationClass="dev.provenance.recorder.wiring.paste.PasteInterceptHandlerFactory"/>
    </extensions>

    <actions>
        <!-- UI trigger for the bundle seal (PRD §4.6), the JetBrains analogue of the VS Code
             recorder's provenance.prepareSubmissionBundle command. Enabled only while a session
             is active; the seal work runs off the EDT (see PrepareSubmissionBundleAction). -->
        <action
            id="dev.provenance.recorder.PrepareSubmissionBundle"
            class="dev.provenance.recorder.commands.PrepareSubmissionBundleAction"
            text="Provenance: Prepare Submission Bundle"
            description="Seal the recorded provenance log and reviewed files into a submission bundle.">
            <add-to-group group-id="ToolsMenu" anchor="last"/>
        </action>

        <!-- Identity (program spec S2). Enrollment is a PASTE, never a fetch: recorder PRD
             NG2 forbids network calls, so the student shows a public key, obtains a token
             out of band, and imports it. Export/Import Student Identity Secret are the only
             way an identity reaches a second machine — the credential vault is not readable
             by hand and there is no escrow. -->
        <action
            id="dev.provenance.recorder.ShowEnrollmentKey"
            class="dev.provenance.recorder.commands.ShowEnrollmentKeyAction"
            text="Provenance: Show My Enrollment Key"
            description="Show your per-course public key to send to course staff.">
            <add-to-group group-id="ToolsMenu" anchor="last"/>
        </action>
        <action
            id="dev.provenance.recorder.ImportEnrollmentToken"
            class="dev.provenance.recorder.commands.ImportEnrollmentTokenAction"
            text="Provenance: Import Enrollment Token"
            description="Paste the enrollment token your course staff sent you.">
            <add-to-group group-id="ToolsMenu" anchor="last"/>
        </action>
        <action
            id="dev.provenance.recorder.ExportStudentSecret"
            class="dev.provenance.recorder.commands.ExportStudentSecretAction"
            text="Provenance: Export Student Identity Secret"
            description="Show your identity secret so you can move it to another machine.">
            <add-to-group group-id="ToolsMenu" anchor="last"/>
        </action>
        <action
            id="dev.provenance.recorder.ImportStudentSecret"
            class="dev.provenance.recorder.commands.ImportStudentSecretAction"
            text="Provenance: Import Student Identity Secret"
            description="Adopt an identity secret exported from another machine.">
            <add-to-group group-id="ToolsMenu" anchor="last"/>
        </action>
    </actions>
</idea-plugin>
